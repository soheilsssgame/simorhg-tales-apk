# حافظهٔ کوتاه نشست‌ها

- ریپو: `soheilsss7/simorgh-tales-apk` — صفحهٔ دانلود + ریلیز APK + اسناد محصول.
- آخرین ریلیز زنده: **v0.6.0-alpha** (`SimorghTales-v0.6.0-alpha.apk` ≈ 4.8MB).
- سورس بازی در این ریپو و در ریپوهای **عمومی** `soheilsss7` نیست.
- ریپوهای عمومی قابل‌دیدن: `simorgh-tales-apk` (فقط README+docs+releases)، `Srip` (پروژهٔ TypeScript نامرتبط)، `Scode` (خالی).
- توکن این نشست به `user/repos` دسترسی ندارد (403)؛ اگر سورس روی ریپوی **خصوصی** باشد از اینجا دیده نمی‌شود تا ریپو به همین اتصال GitHub اضافه شود یا public شود.
- فایل‌های این کلون: فقط `README.md`, `docs/MASTER_PLAN.md`, `docs/MEMORY.md`. هیچ `lib/` یا `pubspec.yaml` یا سورس Flutter/Unity وجود ندارد.
- APKهای ریلیز باینری‌اند نه سورس.
- نقشهٔ کامل: [`docs/MASTER_PLAN.md`](MASTER_PLAN.md)
- شکایت‌های باز P0: فورمیشن ۴ جلو / همپوشانی / پا روی هوا / زمین پوشاننده / انیمیشن‌نبودن حمله / HUD سایتی+ایموجی / نبود کارت+مانا / اتمسفر تخت.
- قانون: کار در گیت‌هاب ذخیره شود تا لیمیت ورک‌اسپیس سند را نکشد.
